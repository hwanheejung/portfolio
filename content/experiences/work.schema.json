{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "work.schema.json",
  "title": "Experience work frontmatter",
  "type": "object",
  "required": [
    "title",
    "slug",
    "date",
    "draft",
    "kind",
    "topics",
    "spotlightIn"
  ],
  "additionalProperties": false,
  "properties": {
    "title": { "type": "string", "minLength": 1 },
    "slug": { "$ref": "#/definitions/slug" },
    "date": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
    },
    "draft": { "type": "boolean" },
    "description": { "type": "string", "minLength": 1 },
    "thumbnail": { "type": "string", "pattern": "^\\./images/" },
    "kind": { "$ref": "#/definitions/slug" },
    "topics": {
      "type": "array",
      "uniqueItems": true,
      "items": { "$ref": "#/definitions/slug" }
    },
    "spotlightIn": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "home": {
          "type": "integer",
          "minimum": 1
        }
      }
    }
  },
  "definitions": {
    "slug": {
      "type": "string",
      "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
    }
  }
}
